---
# Ansible Galaxy requirements for RKE2 installation
# Install with: ansible-galaxy install -r requirements.yml

roles:
  - name: lablabs.rke2
    src: https://github.com/lablabs/ansible-role-rke2.git
    version: v3.0.0  # Use latest stable version
